# nixos-config

## Install

```bash
curl https://raw.githubusercontent.com/wegank/nixos-config/main/naive | sudo bash -s -- netinstall
```

or, equivalently,

```bash
sudo ./naive install
```

## Upgrade

```bash
sudo ./naive upgrade
```
